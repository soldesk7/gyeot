# AGENTS.md — 곁(Gyeot) 공용 에이전트 가이드

이 저장소에서 작업하는 모든 AI 에이전트(Claude Code·Codex·Cursor·Windsurf 등 무엇이든)가 공통으로 지켜야 할 최소 규칙만 담는다. 화면별 담당·스프린트 과제·완료 기준 같은 상세 내용은 여기 넣지 않는다 — 팀 노션 문서가 원본이다. 아래 "개인 설정 파일 만들기"를 참고해 각자 자신의 에이전트용 개인 파일을 만들어 쓴다.

## 프로젝트

**곁**: 응급 순간 골든타임을 놓치지 않도록 돕는, 회원가입 없는 웹서비스. 사진 업로드 → 응급조치 안내 + 119 우선 배너 + 응급실 지도.

- 스택: React(`front/`) + Spring Boot BFF(`back/`), 단일 레포.
- 배포는 하지 않는다 — 개발·시연 모두 로컬 실행.

## 어떤 작업에서도 위반 금지 (불변 원칙)

1. **AI 비진단** — 사진으로 상황을 큰 범주(화상·출혈·외상·의식저하)로 추정만 하고, 사용자에게 확인받기 전에는 확정된 사실처럼 보여주지 않는다. 처치 내용은 AI가 생성하지 않고 공식 기관 자료를 그대로 연결한다.
2. **공식 출처 화이트리스트** — 의료 정보는 소방청·대한심폐소생협회·질병관리청 등 공식 기관 출처만. 임의 콘텐츠 저작 금지.
3. **119·수동 선택 경로 무차단** — AI·네트워크·서버 호출이 119 안내와 수동 선택 가이드를 절대 블로킹하지 않는다(인식은 비동기).
4. **개인정보 최소 수집** — 무계정. 업로드 사진·개인정보는 서버 무저장·무로깅, 인식 후 즉시 폐기.
5. **API 키는 서버에만** — 공공데이터·Gemini 키는 서버 환경변수 전용. 클라이언트 번들·레포 커밋 금지.

## 형상관리 요약 (상세는 팀 노션 "가이드 01 — 형상관리" 참고)

- 모노레포. `front/`는 F1~F5, `back/`는 T 단독 소유 — 남의 폴더는 직접 고치지 않고 담당자에게 요청하거나 이슈로 발주한다.
- GitHub Flow: `main`은 항상 시연 가능한 상태를 유지한다. 브랜치는 `feat/F-xx-설명`·`fix/...`(요구사항 ID 포함) → PR → 머지. `main` 직접 push는 팀장(T)만.
- 이슈 없는 PR 금지. PR 본문에 `Closes #이슈번호` 필수.
- 코드 검토는 리뷰 담당(P)이 전담하고, 승인·머지는 팀장(T)만 한다.
- 라벨은 3종만 쓴다: 담당 코드(T/P/F1~F5) · MoSCoW(must/should/could) · 유형(bug/feature/docs).
- 커밋 전 항상 확인: 키 노출 없음 / 비진단 문구 위반 없음 / 폴더 소유 경계 준수.

## 개인 설정 파일 만들기

팀원별 화면 담당·스프린트 과제·완료 기준 같은 상세 지침은 이 문서에 없다. 팀 노션 워크스페이스의 역할별 가이드 문서(가이드 FE/P/T 등)를 자신의 에이전트에게 읽게 해서, 자기 에이전트에 맞는 이름의 로컬 개인 설정 파일을 만들어 쓴다. 이 파일은 절대 커밋하지 않는다.

- **Claude Code**: `CLAUDE.local.md`를 쓴다 — 자동으로 `.gitignore` 처리되고 `CLAUDE.md`와 함께 자동으로 로드된다. Claude Code는 `AGENTS.md`를 직접 읽지 않으므로, 로컬 `CLAUDE.md` 맨 앞에 `@AGENTS.md` 한 줄을 넣어 이 문서를 가져오게(import) 한다.
- **그 외 도구**(Codex·Cursor·Windsurf 등): 해당 도구가 지원하는 개인 설정 파일 규칙을 따른다 — 대부분 `AGENTS.md`를 직접 읽는다. 

### `AGENTS.md` 임의 수정 금지

이 문서는 팀 전체가 공유하는 공용 파일이다. 내용을 바꿔야 한다고 판단되더라도 에이전트가 임의로 수정하지 않는다 — 사람에게 먼저 확인받고, 팀장(T) 승인을 거쳐 수정한다. 개인적으로 다르게 하고 싶은 부분은 이 문서를 고치지 말고 각자의 로컬 개인 설정 파일(위 "개인 설정 파일 만들기" 참고)에 추가한다.
